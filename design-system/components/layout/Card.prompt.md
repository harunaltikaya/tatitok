Card — the flat surface primitive (solid fill, one hairline border, no shadow). Frames every panel and tile.

```jsx
<Card title="today" actions={<ClassDot tone="live" pulse />}>
  <Stat value="$908.49" sub="141.46M tokens" />
</Card>
```
