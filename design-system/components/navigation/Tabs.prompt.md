Tabs — quiet page switcher; active tab gets a faint surface wash (no underline/accent bar).

```jsx
<Tabs value={page} onChange={setPage}
  tabs={["overview", "explore", "live", "collectors"]} />
```
