FilterChip — removable active-facet chip; click to remove. Uses the neutral brand-green accent.

```jsx
<FilterChip dim="provider" value="anthropic" onRemove={() => remove("provider","anthropic")} />
```
